// cabecera//
public class AnalitzaCaracter {
    public static void main(String[] args) {
        System.out.println("Text?");
        String text = Entrada.readLine();
        System.out.println("Posició?");
        int num = Integer.parseInt(Entrada.readLine());
        char caracter;
        if (num > text.length()) {
            System.out.println("Fora de rang");
        } else if (num < 0) {
            if (num < text.length() *-1) {
                System.out.println("Fora de rang");
            } else {
                caracter = text.charAt(lenght()-num);
            }
        } else {
            character = text.charAt(num);
        }
        analitzaCaracter();
    }

    public static void analitzaCaracter() {
        if (caracter.isLetter()) {
            System.out.println("'" + caracter + "' és una lletra");
        } else if (caracter.isDigit()) {
            System.out.println("'" + caracter + "' és un nombre");
        } else {
            System.out.println("'" + caracter + "' és una altra cosa");
        }
    }
}

li 20Tenia q haber puesto el analitza caracter dentro de los ifs para que me detectase las restricciones sin porblemas

li 9 Tenia que ser num +1 ya que  la longitud siempre es diferente por q empieza en 0

li 20 hay que poner caracter dentro de analitza

li 23 hay q poner char caracter


